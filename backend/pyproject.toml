[build-system]
requires = ["setuptools>=64.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "porfolio"
version = "1.0.0"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic[email]",
    "python-dotenv",
    "gunicorn",
    "httpx",
]
